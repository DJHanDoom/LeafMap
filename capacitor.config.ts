import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.diogo.nervura',
  appName: 'NervuraColetora',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
